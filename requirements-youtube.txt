# Optional deps for scripts/youtube helpers (--openai, --from-json, legacy TTS).
# Preferred path: Cursor skill youtube-text-pack + xAI scripts (stdlib urllib; no pip deps).
# Do NOT add these to requirements.txt (MkDocs CI must stay lean).
openai>=1.40.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
elevenlabs>=1.50.0
